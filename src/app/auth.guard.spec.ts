import { TestBed } from '@angular/core/testing';

import { AuthGuard } from './auth.guard';

describe('AuthGuard', () => {
  let guard: AuthGuard;

  
  it('should be create AuthGuard Function', () => {
    expect(AuthGuard).toBeTruthy();
  });
});
